# Boxarr — setup notes

Boxarr is configured entirely from its web UI. Install **Radarr** from the
Umbrel App Store first, then connect Boxarr to it (URL + API key), pick a quality
profile and root folders. Boxarr keeps the weekly box-office top 10 in sync with
your Radarr library from there.
