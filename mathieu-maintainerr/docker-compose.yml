version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: mathieu-maintainerr_server_1
      APP_PORT: 6246

  server:
    image: ghcr.io/maintainerr/maintainerr:3.23.0@sha256:b6ec7216c5032dd1b8a3aeab8babd167dbeb0794b531fe01fd90b013915db093
    init: true
    environment:
      - TZ=Europe/Paris
    volumes:
      - ${APP_DATA_DIR}/data/opt-data:/opt/data
    restart: on-failure
